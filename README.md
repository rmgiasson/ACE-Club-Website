# ACE-Club-Website

Backend setup:

Create a cluster with MongoDB atlas. Take the connection string and create a .env file in the Backend folder
It should look like this STRING="connection string"

Then run app.js in the Backend directory to start the server
